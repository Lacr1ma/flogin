plugin.tx_login.settings.oneTimeAccount {

    properties {
        # cat=plugin.tx_login//a; type=string; label=One Time user can be initialized with provided list of groups
        usergroup =
    }

    # cat=plugin.tx_login//a; type=int+; label=After defined number of minutes, user will be deleted
    lifetimeInMinutes = 60

}
